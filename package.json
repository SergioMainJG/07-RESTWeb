{
  "name": "07-restweb",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "rimraf": "^5.0.7"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun run --watch src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "bun run build && bun run dist/app.js"
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "env-var": "^7.5.0",
    "express": "4.18.2"
  }
}